version: '3.8'

services:
  manager:
    build:
      context: .
      dockerfile: docker/manager/Dockerfile
    ports:
      - "2933:2933"
    volumes:
      - ./data:/root/data
    restart: always

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "7729:7729"
    depends_on:
      - manager
    restart: always
